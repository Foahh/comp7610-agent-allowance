# 部署指南

## 一、准备钱包

### 1. 安装钱包并创建账户

在浏览器中安装加密货币钱包插件并注册。本文以 MetaMask 为例。

在 MetaMask 中创建两个账户，分别用于以下角色（账户名称可自定义，但需要区分）：

| 账户       | 用途                                      |
| ---------- | ----------------------------------------- |
| `agent`    | Agent 使用的账户，需要持有 Sepolia ETH    |
| `provider` | Provider 使用的账户，无需持有 Sepolia ETH |

![在 MetaMask 中创建账户](assets/image.png)

### 2. 领取 Sepolia ETH

复制 `agent` 账户的钱包地址。

![复制钱包地址](assets/image-1.png)

前往 [Google Cloud Sepolia 水龙头](https://cloud.google.com/application/web3/faucet/ethereum/sepolia)，免费领取 Sepolia ETH。

![领取 Sepolia ETH](assets/image-2.png)

### 3. 获取账户私钥

分别进入两个账户的 **Account Details**，复制私钥，供后续配置环境变量使用。

![打开账户详情](assets/image-4.png)

![获取账户私钥](assets/image-5.png)

## 二、准备 AI 服务

前往 [DeepSeek 开放平台](https://platform.deepseek.com/usage)，爆米并获取 API Key。

## 三、获取项目

克隆仓库：

```sh
git clone https://github.com/Foahh/comp7610-agent-allowance
```

使用你熟悉的 IDE 打开克隆后的项目文件夹。

## 四、配置环境变量

### 1. 创建配置文件

将仓库中的 `.env.example` 复制为 `.env`，然后按下面的说明填写相关字段。

![环境变量配置示例](assets/image-3.png)

### 2. 配置 AI 服务

| 变量              | 填写说明                           |
| ----------------- | ---------------------------------- |
| `OPENAI_BASE_URL` | DeepSeek API 地址                  |
| `OPENAI_API_KEY`  | 你获取的 DeepSeek API Key          |
| `OPENAI_MODEL`    | 模型名称，推荐 `deepseek-v4-flash` |

配置示例：

```dotenv
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=sk-...
OPENAI_MODEL=deepseek-v4-flash
```

### 3. 配置钱包

填写前面获取的账户私钥：

| 变量                   | 填写说明                                               |
| ---------------------- | ------------------------------------------------------ |
| `DEPLOYER_PRIVATE_KEY` | 智能合约部署账户私钥，与 `AGENT_PRIVATE_KEY` 相同      |
| `AGENT_PRIVATE_KEY`    | `agent` 账户私钥，该账户必须持有前面领取的 Sepolia ETH |
| `PROVIDER_PRIVATE_KEY` | `provider` 账户私钥，该账户无需持有 Sepolia ETH        |

配置示例：

```dotenv
DEPLOYER_PRIVATE_KEY=0x...
AGENT_PRIVATE_KEY=0x...
PROVIDER_PRIVATE_KEY=0x...
```

## 五、安装依赖

前往 [Vite+ 官方安装指南](https://viteplus.dev/guide/)，按说明安装。
Vite+ 会管理运行时，无需单独安装 Node.js 或 pnpm。

安装后重新打开终端，在项目根目录运行：

```sh
vp install
```

## 六、部署智能合约

确保部署账户 `DEPLOYER_PRIVATE_KEY` 中有 Sepolia ETH，然后依次运行：

```sh
vp run @repo/contracts#build
vp run deploy:sepolia
```

## 七、启动应用

```sh
vp run db:init
vp run dev
```

保持终端运行。之后再次启动只需运行 `vp run dev`。

## 八、开始使用

将钱包切换到 Sepolia，账户为 `agent`，并点击 `Connect wallet`。钱包中需要有 Sepolia ETH 来支付手续费。

![alt text](assets/image-6.png)

在页面领取 ATT 测试代币，设置消费额度并完成授权，然后打字聊天即可。
