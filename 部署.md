# 部署指南

## 一、准备钱包

安装 MetaMask 等浏览器钱包，创建账户，并切换到 **Ethereum Sepolia**。

复制钱包地址，前往 [Google Cloud 水龙头](https://cloud.google.com/application/web3/faucet/ethereum/sepolia)免费领取 Sepolia ETH。

![复制钱包地址](assets/image-1.png)

![领取 Sepolia ETH](assets/image-2.png)

## 二、准备 AI 服务

前往 [DeepSeek 开放平台](https://platform.deepseek.com/usage)，爆米并获取 API Key，稍后在应用中填写。

## 三、获取项目

```sh
git clone https://github.com/Foahh/comp7610-agent-allowance
cd comp7610-agent-allowance
```

使用你熟悉的 IDE 打开项目文件夹。

## 四、配置环境变量

将 `.env.example` 复制为 `.env`，把 `OWNER_ADDRESS` 填为浏览器钱包地址，其他配置暂时保持默认。

```dotenv
OWNER_ADDRESS=0x...
```

如果组内已有共用合约，同时填写 `TOKEN_ADDRESS` 和 `VAULT_ADDRESS`。

## 五、安装依赖

按照 [Vite+ 安装指南](https://viteplus.dev/guide/)完成安装，然后在项目根目录重新打开终端：

```sh
vp install
vp run accounts
```

应用会在本地生成后端密钥并显示公开地址。给 `agent` 和 `deployer` 地址转入 Sepolia ETH，无需在 `.env` 填写私钥。

## 六、部署智能合约

```sh
vp run @repo/contracts#build
vp run deploy:sepolia
```

合约地址保存在 `data/deployment-11155111.json`。已配置共用合约可跳过部署；互相交易的参与者必须使用同一套合约。

## 七、启动应用

```sh
vp run db:init
vp run dev
```

打开 `http://localhost:3000`，保持终端运行。之后再次启动只需运行 `vp run dev`。

## 八、开始使用

连接 `OWNER_ADDRESS` 对应的钱包并签名登录。

在 **Settings** 填写 DeepSeek 接口地址、模型名称和 API Key，点击 **Check connection**，再选择买方默认模型。

在 **My listings** 发布商品，或在 **Connected sellers** 添加其他参与者的 API 地址。在 **Chat** 选择卖家，按提示领取测试 ATT 并授权消费额度，即可开始购买。
