{
  "name": "@repo/contracts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp exec hardhat compile",
    "deploy": "vp exec node scripts/deploy.ts",
    "deploy:sepolia": "vp exec node scripts/deploy.ts"
  },
  "dependencies": {
    "@openzeppelin/contracts": "catalog:",
    "@repo/schemas": "workspace:*",
    "@repo/utils": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "hardhat": "catalog:",
    "vite-plus": "catalog:"
  }
}
